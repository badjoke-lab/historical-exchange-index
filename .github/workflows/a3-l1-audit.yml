name: A3 L1 audit

on:
  pull_request:
    branches:
      - main
  workflow_dispatch:

jobs:
  audit:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
      - run: npm ci
      - run: node scripts/export-lineage-l1-context.mjs --output=audit-output/a3-l1-audit.json
      - uses: actions/upload-artifact@v4
        with:
          name: a3-l1-audit
          path: audit-output/a3-l1-audit.json
          retention-days: 3
