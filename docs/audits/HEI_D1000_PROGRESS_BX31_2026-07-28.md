# HEI D-1000 Progress Checkpoint — BX31

Date: 2026-07-28  
Status: IN PROGRESS  
Project: Historical Exchange Index (HEI)

## 1. Starting state

BX31 starts from current `main` commit `71a5bfca7da9e5e58be96713b972c8ab25caa387` after the reviewed BX30 merge.

```text
Entities: 919
Events:   1014
Evidence: 3608
```

The values use reviewed public aggregation semantics rather than base-array lengths.

## 2. Direct overlap repair

The initial review set also considered CoinTR and CoinUp.io. Direct current-main canonical-path checks found both already represented:

```text
CoinTR    -> hei_ex_000645
CoinUp.io -> hei_ex_000646
```

Neither existing entity was duplicated. The replacement candidates were reviewed against current first-party and independent or registry evidence before drafting.

## 3. Final batch

```text
Coinlocally hei_ex_001040 limited
Websea      hei_ex_001041 active
BitMake     hei_ex_001042 limited
Bitowex     hei_ex_001043 limited
```

BX31 adds no lifecycle event. The next event ID remains `hei_ev_010091`.

## 4. Projected reviewed state

```text
Entities: 923
Events:   1014
Evidence: 3616
```

```text
Entities: +4
Events:   +0
Evidence: +8
```

```text
English dossiers:  923
Japanese dossiers: 923
Sitemap routes:     1890
Remaining to D-1000: 77
```

## 5. Status and evidence discipline

Coinlocally is `limited`, not active or dead. Its current first-party site remains available, but the platform states that deposits and withdrawals are temporarily suspended during a custody and transfer infrastructure upgrade without a completion time.

Websea is `active` from current first-party spot and derivatives surfaces plus current independent market activity. Its 2023 launch marker remains year-level only, and confidence remains `medium` because the public contracting operator was not fully resolved.

BitMake is `limited`. Current independent exchange-directory material continues to identify the platform and reported reserves, but volume is untracked and usable current market data is absent. The first-party root URL remains `live_unverified` rather than overstated as fully verified.

Bitowex is `limited`. Current first-party trading, funding, wallet, and legal surfaces are available, and the disclosed UK company is active in the official company register. That register does not independently verify exchange operations, so HEI does not use `active` and leaves launch date unset.

No unsupported terminal state, exact launch day, licensing conclusion, legal lineage, predecessor, successor, or artificial entity split is introduced.

## 6. Boundaries

BX31 changes reviewed exchange bundles and checkpoint authority only. It does not change L-2 HOLD, Japanese Pilot scope, third-language authorization, schemas, machine-readable safety rules, Cloudflare configuration, route contracts, or production verification policy.

## 7. Validation gate

The final head must pass Records validation, Backlog dedupe, Candidate scan, Watchlist resolution, Count semantics, Maintainer recovery, Machine/public consistency, Japanese Pilot readiness, L2 localization evaluation, URL safety, Country origin, CI, and the remaining normal HEI audits.

Any duplicate, ID collision, reference-integrity, count, URL-safety, enum, lifecycle, country, or recovery-contract failure must be repaired rather than bypassed.
