# HEI D-1000 Progress Checkpoint — BX28

Date: 2026-07-25  
Status: IN PROGRESS  
Project: Historical Exchange Index (HEI)

## 1. Current milestone

```text
D-1000 Reviewed Entity Milestone
```

BX28 continues reviewed canonical growth from BX27 merged as `67fd9f0646bfb351bb1ae33ba599535ccd418209`.

## 2. Batch contents

```text
Arkham Exchange hei_ex_001026 limited
YoBit            hei_ex_001027 active
Hodl Hodl        hei_ex_001028 active
Paxful           hei_ex_001029 dead
```

Paxful also adds:

```text
hei_ev_010082 shutdown_announced 2025-10-01
hei_ev_010083 shutdown_effective 2025-11-01
```

## 3. Projected reviewed state

```text
Entities: 909
Events:   1007
Evidence: 3580
```

Batch delta:

```text
Entities: +4
Events:   +2
Evidence: +9
```

Remaining to D-1000:

```text
91 reviewed entities
```

Projected localized and route coverage:

```text
English dossiers:  909
Japanese dossiers: 909
Sitemap routes:     1866
```

## 4. Status discipline

Arkham Exchange is `limited` because current first-party documentation establishes centralized spot and perpetual-futures infrastructure together with decentralized Solana trading, while current independent centralized-exchange tracking reports no tracked pairs or volume. Centralized and decentralized Arkham trading surfaces remain one hybrid entity.

YoBit is `active` because current first-party market pages expose thousands of trading pairs with non-zero quoted activity and current independent market data lists recently updated spot markets and non-zero volume. Confidence remains medium because ownership and jurisdiction are opaque and independent quality warnings remain material.

Hodl Hodl is `active` because current first-party pages and API documentation expose an operating non-custodial peer-to-peer Bitcoin marketplace using multisignature escrow. The bounded `dex` type reflects direct user-to-user trading without exchange custody.

Paxful is `dead` because current first-party materials confirm a 2025-10-01 wind-down announcement and the 2025-11-01 effective end of trading, deposits, sending, and receiving. Withdrawal-only access does not qualify as an operating exchange.

No unsupported launch date, legal jurisdiction, predecessor, successor, or incident event is invented.

## 5. Identity and overlap findings

Direct checks preserved these entity-first boundaries:

- Arkham's centralized exchange and decentralized Solana trading remain one hybrid entity.
- YoBit and YoBit.Net remain one centralized exchange entity.
- Hodl Hodl's marketplace, offers, contracts, and API remain one non-custodial exchange entity.
- Paxful's former marketplace and current withdrawal-only wind-down surface remain one terminal entity.

Adjacent backlog noise was not duplicated. The standalone `Coinbase` seed remains represented by `Coinbase Exchange`, and `One Trading` remains represented by the existing `Bitpanda Pro` identity found during BX27 validation.

Records validation, Backlog dedupe, Candidate scan, Watchlist resolution, and reviewed lifecycle semantics remain authoritative.

## 6. Localization and machine-readable boundaries

BX28 changes reviewed exchange bundles and checkpoint authorities only.

It does not change:

- L-2 HOLD decision;
- Japanese Pilot scope;
- third-language authorization;
- schema definitions;
- machine-readable safety rules;
- Cloudflare deployment configuration;
- production verification policy.

## 7. Validation gate

Before merge, the final BX28 head must pass all normal HEI workflows, including:

```text
Records validation
Backlog dedupe
Candidate scan gate
Watchlist resolution gate
Count semantics regression
Maintainer recovery gate
Machine/public consistency gate
Japanese Pilot readiness gate
L2 localization evaluation gate
CI
```

Any overlap, ID collision, reference-integrity, count, URL-safety, status, lifecycle, or contract failure must be repaired rather than bypassed.
